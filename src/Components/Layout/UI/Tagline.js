const Tagline = (props) => (
    <div style={{ "margin": "6em 0", "textAlign": "center", "color": "#0019FF" }}>
        <h1>
            {
                props.children
            }
        </h1>
    </div>
)
export default Tagline